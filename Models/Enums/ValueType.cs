namespace Arion.ClaimsSignup.SampleClient.Models.Enums
{
    public enum ValueType
    {
        /// <summary>
        /// If the amount is a percentage
        /// </summary>
        Percentage,

        /// <summary>
        /// If the amount is a fixed amount
        /// </summary>
        FixedAmount
    }
}
